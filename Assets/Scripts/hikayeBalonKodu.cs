using UnityEngine;
using UnityEngine.SceneManagement;

public class StoryBubbleController : MonoBehaviour
{
 /*   public GameObject storyBubble;

    private void OnEnable()
    {
        SceneManager.sceneLoaded += OnSceneLoaded;
    }

    private void OnDisable()
    {
        SceneManager.sceneLoaded -= OnSceneLoaded;
    }

    private void OnSceneLoaded(Scene scene, LoadSceneMode mode)
    {
        if (scene.name == "yeraltisavas") // Hedef sahnenin ismini burada belirt
        {
            ShowStoryBubble();
        }
    }

    void ShowStoryBubble()
    {
        Time.timeScale = 0f;
        storyBubble.SetActive(true);
    }

    public void HideStoryBubble()
    {
        storyBubble.SetActive(false);
        Time.timeScale = 1f;
    }       */      
}

